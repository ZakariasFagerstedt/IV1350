package model;

/**
 * Object used to keep track of Customer-Identifier, used for discounts
 */
public class Customer {

    private int identifier;

    /**
     * Constructor for customer-identifier
     * @param identifier the Customer-identifier
     */
    public Customer(int identifier) {
        this.identifier = identifier;
    }
}
